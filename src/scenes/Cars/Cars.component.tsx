import React, {FunctionComponent, useEffect} from "react";
import {ICar} from "../../stores/CarsStore/Cars.types";

type IProps = {
  data: ICar[] | null
};

const CarsComponent: FunctionComponent<IProps> = () => {

  return (
    <div>

    </div>
  );
};

export default CarsComponent;
